
REMOVED ANCHORORGIN FROM PROPS LIST IN COMPONENTS/TOOLBAR.JS TO FIX WARNING IN THE CONSOLE

 <Menu
   id="menu-appbar"
   anchorEl={anchorElement}
   anchorOrigin={{
           vertical: 'bottom',
            horizontal: 'left',
       }}
   keepMounted
   transformOrigin={{
        vertical: 'bottom',
        horizontal: 'left',
  }}
   open={menuOpen}
   onClose={closeMenu}
  >